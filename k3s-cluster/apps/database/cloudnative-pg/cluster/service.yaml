---
apiVersion: v1
kind: Service
metadata:
  name: postgres16-loadbalancer
  labels:
    postgresql: postgres
    role: primary
  annotations:
    metallb.universe.tf/loadBalancerIPs: ${METALLB_POSTGRES}
spec:
  ports:
    - protocol: TCP
      port: 5432
  selector:
    postgresql: postgres
    role: primary
  type: LoadBalancer