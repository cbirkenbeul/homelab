---
- name: Create AdGuard Home LXC
  community.general.proxmox:
    vmid: 110
    node: pve02
    api_user: "{{ SECRET_API_USER }}"
    api_password: "{{ SECRET_API_PASSWORD }}"
    api_host: pve02
    hostname: adg01
    ostemplate: iso:vztmpl/ubuntu22-docker.tar.gz