---
- hosts:
    - bkp01
  become: true
  gather_facts: true
  any_errors_fatal: true
  pre_tasks:
    - name: Pausing for 5 seconds...
      pause:
        seconds: 5
  tasks:
    - name: Create docker.yaml file
      ansible.builtin.copy:
        content: |
          [backblaze]
          type = b2
          account = 002265ae0c8725b000000000e
          key = K0023DHzF6YxUwUWqBlJ1ud9JxsOM3s
          hard_delete = true
          #endpoint = https://s3.us-west-002.backblazeb2.com
          versions = true
        dest: "{{ docker_dir }}/homepage/data/docker.yaml"
        mode: '0644'
        owner: "{{ username }}"
        group: "{{ username }}"